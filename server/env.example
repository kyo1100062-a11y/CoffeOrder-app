# ============================================
# 서버 환경 변수 설정
# ============================================
# 이 파일을 .env로 복사하고 실제 값으로 수정하세요
# cp env.example .env

# --------------------------------------------
# 서버 포트 설정
# --------------------------------------------
PORT=3001

# --------------------------------------------
# PostgreSQL 데이터베이스 설정
# --------------------------------------------
# 데이터베이스 서버 주소 (로컬 개발 시 localhost)
DB_HOST=localhost

# PostgreSQL 포트 (기본값: 5432)
DB_PORT=5432

# 데이터베이스 이름
DB_NAME=coffee_order

# PostgreSQL 사용자 이름
DB_USER=postgres

# PostgreSQL 비밀번호
# ⚠️ 중요: 실제 PostgreSQL 비밀번호로 변경하세요!
# 특수문자(!, @, #, $ 등)가 있어도 그대로 입력하면 됩니다.
# 따옴표나 공백 없이 입력하세요.
DB_PASSWORD=your_postgresql_password_here

